apply plugin: 'com.android.library'

android {
    namespace "org.ludeme.ludii.core"
    compileSdk 34

    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 35
        versionCode 1
        versionName "1.0.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
    compileOptions {
        sourceCompatibility rootProject.ext.java_version
        targetCompatibility rootProject.ext.java_version
    }

}

dependencies {
    implementation 'androidx.core:core:1.13.1'
}

